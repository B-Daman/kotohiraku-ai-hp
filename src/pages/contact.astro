---
import BaseLayout from "../layouts/BaseLayout.astro";
import { socialLinks } from "../data/siteConfig";
---

<BaseLayout title="お問い合わせ" description="ことひらくAIへのお問い合わせ。AI導入相談、セミナー、訪問サポートについてお気軽にご連絡ください。">
  <!-- ページヘッダー -->
  <section class="bg-primary py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 text-center">
      <h1 class="text-3xl md:text-4xl font-bold text-white mb-4">お問い合わせ</h1>
      <p class="text-white/70 max-w-2xl mx-auto">
        AI活用についてのご相談、お気軽にお問い合わせください。初回相談は無料です。
      </p>
    </div>
  </section>

  <section class="py-16 md:py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- フォーム -->
        <div class="md:col-span-2">
          <div class="bg-white rounded-2xl p-8 border border-base-dark">
            <h2 class="text-xl font-bold text-primary mb-6">お問い合わせフォーム</h2>
            <p class="text-text-light text-sm mb-6">
              以下のGoogleフォームからお問い合わせください。
              通常2営業日以内にご返信いたします。
            </p>
            <!-- Googleフォーム埋め込みプレースホルダー -->
            <div class="bg-base rounded-xl p-12 text-center border border-base-dark">
              <p class="text-text-lighter text-sm mb-4">
                Googleフォームを準備中です
              </p>
              <p class="text-text-light text-sm">
                お急ぎの場合はSNSからご連絡ください
              </p>
            </div>
          </div>
        </div>

        <!-- サイドバー -->
        <div class="space-y-6">
          <!-- SNSリンク -->
          <div class="bg-white rounded-2xl p-6 border border-base-dark">
            <h3 class="font-bold text-primary mb-4">SNSでつながる</h3>
            <div class="space-y-3">
              {socialLinks.map((link) => (
                <a
                  href={link.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="flex items-center gap-3 text-text-light hover:text-accent transition-colors text-sm py-2"
                >
                  <span class="w-8 h-8 bg-primary/10 rounded-lg flex items-center justify-center text-primary text-xs font-bold">
                    {link.icon === "x" ? "X" : "N"}
                  </span>
                  <span>{link.name}</span>
                  <span class="ml-auto text-text-lighter">↗</span>
                </a>
              ))}
            </div>
          </div>

          <!-- お問い合わせ内容の例 -->
          <div class="bg-white rounded-2xl p-6 border border-base-dark">
            <h3 class="font-bold text-primary mb-4">こんなことを相談できます</h3>
            <ul class="space-y-2 text-sm text-text-light">
              <li class="flex items-start gap-2">
                <span class="text-accent mt-0.5">●</span>
                <span>AIをどう業務に活かせるか知りたい</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-accent mt-0.5">●</span>
                <span>セミナーを開催してほしい</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-accent mt-0.5">●</span>
                <span>パソコンの設定で困っている</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-accent mt-0.5">●</span>
                <span>ChatGPTの使い方を教えてほしい</span>
              </li>
              <li class="flex items-start gap-2">
                <span class="text-accent mt-0.5">●</span>
                <span>AI導入の継続サポートがほしい</span>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>