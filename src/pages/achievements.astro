---
import BaseLayout from "../layouts/BaseLayout.astro";
import {
  seminarStats,
  govResults,
  mediaAppearances,
  communityStats,
} from "../data/siteConfig";
---

<BaseLayout title="実績" description="AIセミナー40回以上開催、270名以上が参加。役場の議会答弁作成時間90%削減など、具体的な成果をご紹介します。">
  <!-- ページヘッダー -->
  <section class="bg-primary py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 text-center">
      <h1 class="text-3xl md:text-4xl font-bold text-white mb-4">実績・メディア</h1>
      <p class="text-white/70 max-w-2xl mx-auto">
        琴平町でのAI活用支援の実績と、メディア掲載をご紹介します
      </p>
    </div>
  </section>

  <!-- AIセミナー実績 -->
  <section class="py-16 md:py-24">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <h2 class="text-2xl md:text-3xl font-bold text-primary mb-8">AIセミナー実績</h2>
      <p class="text-text-light mb-8 max-w-3xl">
        2025年5月から、琴平町内で毎週2回のAIセミナーを継続開催。
        ITリテラシーに関わらず、どなたでも参加できる実践型の内容です。
      </p>
      <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
        {seminarStats.map((stat) => (
          <div class="bg-white rounded-xl p-6 border border-base-dark text-center">
            <div class="text-xl md:text-2xl font-bold text-primary mb-1">{stat.value}</div>
            <div class="text-xs text-text-light">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <hr class="border-base-dark max-w-6xl mx-auto" />

  <!-- 役場AI導入成果 -->
  <section class="py-16 md:py-24">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <h2 class="text-2xl md:text-3xl font-bold text-primary mb-8">役場AI導入の成果</h2>
      <p class="text-text-light mb-8 max-w-3xl">
        琴平町役場へのAI導入支援により、具体的な業務改善を実現しました。
      </p>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {govResults.map((result) => (
          <div class="bg-white rounded-xl p-8 border border-base-dark">
            <h3 class="font-bold text-primary mb-4">{result.label}</h3>
            {"before" in result ? (
              <div>
                <div class="flex items-center gap-3 mb-2">
                  <span class="text-text-lighter line-through text-lg">{result.before}</span>
                  <span class="text-accent text-xl">→</span>
                  <span class="text-primary font-bold text-2xl">{result.after}</span>
                </div>
                <div class="inline-block bg-accent/10 text-accent font-bold text-sm px-3 py-1 rounded-full">
                  {result.reduction}
                </div>
              </div>
            ) : (
              <div class="text-primary font-bold text-xl">{result.result}</div>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <hr class="border-base-dark max-w-6xl mx-auto" />

  <!-- コミュニティ -->
  <section class="py-16 md:py-24 bg-white">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <h2 class="text-2xl md:text-3xl font-bold text-primary mb-8">コミュニティ</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-2xl">
        {communityStats.map((stat) => (
          <div class="rounded-xl p-6 border border-base-dark text-center">
            <div class="text-2xl md:text-3xl font-bold text-primary mb-1">{stat.value}</div>
            <div class="text-sm text-text-light">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <hr class="border-base-dark max-w-6xl mx-auto" />

  <!-- メディア掲載 -->
  <section class="py-16 md:py-24">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <h2 class="text-2xl md:text-3xl font-bold text-primary mb-8">メディア掲載</h2>
      <div class="bg-white rounded-2xl border border-base-dark overflow-hidden">
        <div class="overflow-x-auto">
          <table class="w-full text-sm">
            <thead>
              <tr class="border-b border-base-dark bg-base">
                <th class="text-left px-6 py-4 font-bold text-primary">メディア</th>
                <th class="text-left px-6 py-4 font-bold text-primary">種別</th>
                <th class="text-left px-6 py-4 font-bold text-primary">時期</th>
              </tr>
            </thead>
            <tbody>
              {mediaAppearances.map((media, i) => (
                <tr class:list={[
                  "border-b border-base-dark last:border-b-0",
                  i % 2 === 0 ? "" : "bg-base/50",
                ]}>
                  <td class="px-6 py-4 font-medium text-primary">{media.name}</td>
                  <td class="px-6 py-4 text-text-light">{media.type}</td>
                  <td class="px-6 py-4 text-text-light">{media.date || "—"}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>